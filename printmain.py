'''
  if i in [2]:
    for k in [11]:
      for p in [19]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [11]:
    for k in [13]:
      for p in [21]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [2]:
    for k in [15]:
      for p in [21]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [11]:
    for k in [15]:
      for p in [22]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [2]:
    for k in [13]:
      for p in [22]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [13]:
    for k in [15]:
      for p in [19]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [11]:
    for k in [13]:
      for p in [21]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [2]:
    for k in [19]:
      for p in [29]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [15]:
    for k in [22]:
      for p in [29]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [13]:
    for k in [21]:
      for p in [29]:
        sampling=10;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [19]:
    for k in [21]:
      for p in [22]:
        sampling=5;
        for j in range(-sampling,sampling+1):
          for m in range(-sampling,sampling+1):
            for q in range(-sampling,sampling+1):
              if i==29:
                scale1=1.0;
              else:
                scale1=4.0;
              if k==29:
                scale2=1.0;
              else:
                scale2=4.0;
              if p==29:
                scale3=1.0;
              else:
                scale3=4.0;
              dp=(j/(sampling+0.0)*vmerge[i]*scale1+m/(sampling+0.0)*vmerge[k]*scale2+q/(sampling+0.0)*vmerge[p]*scale3);
              dp=dp/(np.sqrt(np.array(mass3D)));
              dp2D=dp.reshape(natom,3);
              positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
  if i in [2]:
    for k in [13]:
      for p in [19]:
        for t in [21]:
          print("t=",t);
          sampling=10;
          for n in range(-sampling,sampling+1):
            for j in range(-sampling,sampling+1):
              for m in range(-sampling,sampling+1):
                for q in range(-sampling,sampling+1):
                  if i==29:
                    scale1=1.0;
                  else:
                    scale1=4.0;
                  if k==29:
                    scale2=1.0;
                  else:
                    scale2=4.0;
                  if p==29:
                    scale3=1.0;
                  else:
                    scale3=4.0;
                  if t==29:
                    scale4=1.0;
                  else:
                    scale4=4.0;
                  dp=(n/(sampling+0.0)*vmerge[i]*scale1+j/(sampling+0.0)*vmerge[k]*scale2+m/(sampling+0.0)*vmerge[p]*scale3+q/(sampling+0.0)*vmerge[t]*scale4);
                  dp=dp/(np.sqrt(np.array(mass3D)));
                  dp2D=dp.reshape(natom,3);
                  positdiff.writeinterpolate(natom,axis,dp2D+ExpandPosition,"./phase/cubic","./MODEFIT/"+"MODE"+str(i)+"MODE"+str(k)+"MODE"+str(p)+"MODE"+str(t)+"INTER"+str(n)+"INTER"+str(j)+"INTER"+str(m)+"INTER"+str(q));
